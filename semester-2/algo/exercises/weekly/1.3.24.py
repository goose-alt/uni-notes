"""
    removeAfter()
    Check if the next element is the last
    If it isnt then set the current nodes next reference to the node after the next
    Aka, if the current node is called node, then:
    node.next = node.next.next
"""